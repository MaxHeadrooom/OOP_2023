#include <bits/stdc++.h>
#include "figure.h"
#include "triangle.h"
#include "array.h"

int main()
{
    arrayy a(10);
    triangle* p2 = new triangle();
    a.push_back(0, p2);
    /*try
    {
    }
    catch(const exception& e)
    {
        std::cerr << e.what() << '\n';
    }*/
}